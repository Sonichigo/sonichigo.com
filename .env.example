# GitHub API Token (optional, but recommended to avoid rate limiting)
GITHUB_TOKEN=your_github_token_here

# Optional: Neon Database URL (only if you want to use the existing db features)
# The site works fully with IndexedDB + markdown backups without this
DATABASE_URL=postgresql://user:password@host/database
